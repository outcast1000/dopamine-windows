<UserControl
    x:Class="Dopamine.Views.Common.EditArtistBiographyControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:pc="clr-namespace:Dopamine.Controls"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="300">
    <DockPanel>
        <TextBlock
            DockPanel.Dock="Top"
            FontSize="20"
            Text="{DynamicResource Language_Biography}"
            Foreground="{DynamicResource Brush_PrimaryText}"
            Margin="0,5,0,10" />
        <pc:AccentTextBox
            VerticalContentAlignment="Top"
            TextWrapping="Wrap"
            AcceptsReturn="True"
            VerticalScrollBarVisibility="Auto"
            Text="{Binding ArtistBiography.Value, UpdateSourceTrigger=PropertyChanged}"
            ShowAccent="{Binding ArtistBiography.IsValueChanged}" />
    </DockPanel>
</UserControl>
